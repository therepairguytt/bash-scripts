These scripts will install NGINX, NGINX Extras, curl, wget, certbot and the certbot plugin for NGINX.

Use the following commands to download and run the first script.

Download and run script:

sudo wget https://raw.githubusercontent.com/therepairguytt/bash-scripts/refs/heads/NGINX-Scripts/install_nginx.sh && chmod +x install_nginx.sh && ./install_nginx.sh